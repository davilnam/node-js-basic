<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update user</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="update-user-container">
        <form action="/update-user" method="post">
            <div>
                <label for="firstName">First Name</label>
                <input type="text" id="firstName" value="<%= dataUser.firstName %>" name="firstName" required \>
            </div>
            <div>
                <label for="lastName">Last Name</label>
                <input type="text" id="lastName" value="<%= dataUser.lastName %>" name="lastName" required \>
            </div>
            <div>
                <label for="email">Email</label>
                <input type="email" id="email" value="<%= dataUser.email %>" name="email" required \>
            </div>
            <div>
                <label for="address">Address</label>
                <input type="text" id="address" value="<%= dataUser.address %>" name="address" required \>
            </div>
            <input type="text" hidden name="id" value="<%= dataUser.id %>" />
            <div>
                <button type="submit" class="btn-save">Submit</button>
            </div>


        </form>
    </div>
</body>

</html>